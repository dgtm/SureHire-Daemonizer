# -*- ruby -*-

require 'rubygems'
require 'hoe'

# Hoe.plugin :compiler
# Hoe.plugin :gem_prelude_sucks
# Hoe.plugin :inline
# Hoe.plugin :minitest
# Hoe.plugin :racc
Hoe.plugin :bundler
# Hoe.plugin :rubyforge

Hoe.spec 'surehire_daemonizer' do
  # HEY! If you fill these out in ~/.hoe_template/Rakefile.erb then
  # you'll never have to touch them again!
  # (delete this comment too, of course)

  developer('dgtm', 'dipeshgtm@gmail.com')
  # self.add_dependencies "foreverb"
  # self.rubyforge_name = 'surehire_daemonizerx' # if different than 'surehire_daemonizer'
end

# vim: syntax=ruby
